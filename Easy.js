var run = function exercise(activity) {
    activity = "running"
    return `Today's exercise: ${activity}`;
}

var swim = function exercise(activity) {
    activity = "swimming"
    return `Today's exercise: ${activity}`;
}

console.log(run());
console.log(swim());